# Tasks

- [x] Add `CLAUDE.md` with the TypeScript and Yarn stack, scripts, and validation guidance.
- [x] Document local bot startup, required variables, and MongoDB usage.
- [x] Document project structure and Git branch/commit conventions.
- [x] Correct `Application` and `Domain` import path casing for case-sensitive filesystems.
- [x] Run `yarn install` and verify documented build command.
